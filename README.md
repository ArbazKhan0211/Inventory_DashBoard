# Inventory_DashBoard
A full-stack app to upload a stock-movements JSON file, verify its integrity with a client-computed SHA-256 digest, and explore the data through a filterable, paginated table plus a pie chart and a time-series chart.
